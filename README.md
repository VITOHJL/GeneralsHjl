# Generals.io - 本地单机游戏

一个基于 generals.io 游戏规则的本地单机策略游戏。支持人机对战和AI对战，包含5种AI智能体（Random、Adaptive、Minimax、Q-Learning、StrategyRL）。

### 🌐 在线试玩：[http://generals.zech.xin](http://generals.zech.xin)

---

## 快速开始

### 1. 环境要求

- **Node.js**: 16+ 版本
- **npm**: 或 yarn 包管理器

### 2. 下载和安装

```bash
# 克隆或下载项目到本地
cd generals.io

# 安装依赖
npm install
```

### 3. 运行游戏

```bash
# 启动开发服务器
npm run dev
```

开发服务器将在 `http://localhost:1732` 启动。在浏览器中打开即可开始游戏。

### 4. 构建生产版本

```bash
# 构建
npm run build

# 预览构建结果
npm run preview
```

---

## 命令行测试工具

### AI 对战测试（锦标赛）

运行所有AI之间的锦标赛，测试在不同地图尺寸下的表现：

```bash
# Windows PowerShell
node src/training/tournament.js

# Linux / macOS
node src/training/tournament.js
```

**输出**：控制台显示统计表格 + `tournament_results.json` 文件

**配置**：可在 `src/training/tournament.js` 中修改：
- `GAMES_PER_MAP`: 每个地图尺寸的游戏数量（默认300）
- `MAP_SIZES`: 地图尺寸列表（默认 15x15, 20x20, 25x25）
- `MAX_TURNS`: 最大回合数（默认5000）

### AI vs RANDOM 测试

测试各个AI与RANDOM AI进行1v1对战的胜率：

```bash
# Windows PowerShell
node src/training/test_vs_random.js

# 自定义参数
node src/training/test_vs_random.js --games 500 --map-size 15
```

**输出**：控制台显示统计表格 + `test_vs_random_results.json` 文件

**默认配置**：
- 地图尺寸：15x15
- 每个AI游戏数：500局
- 最大回合：5000

### 分析锦标赛结果

如果已经运行过锦标赛并保存了结果，可以直接分析：

```bash
# 分析现有结果
node src/training/analyze_tournament.js

# 分析并保存清理后的版本（移除详细数据，只保留统计）
node src/training/analyze_tournament.js --save-clean
```

**输入**：`tournament_results.json`

**输出**：控制台显示格式化的统计表格 + `tournament_results_clean.json`（可选）

### 强化学习训练

#### 训练 StrategyRL AI

```bash
# 基础训练（对 random 对手）
node src/training/train_rl.js --agent strategy --episodes 2000 --players 2 --width 10 --height 10 --maxTurns 500 --mode against-fixed --opponent random --savePath ./qtable-strategy.json

# 进阶训练（对 adaptive 对手，继续之前的Q表）
node src/training/train_rl.js --agent strategy --episodes 2000 --opponent adaptive --loadPath ./qtable-strategy.json --savePath ./qtable-strategy-adaptive.json
```

#### 训练 QLearning AI

```bash
# 使用专用训练脚本
node src/training/train_qlearning.js --episodes 2000 --opponent random --savePath ./qtable-qlearning.json
```

### AI 对比测试

对比两个AI的直接对战表现：

```bash
# 对比 Adaptive 和 Random
node src/training/train.js compare --ai1 adaptive --ai2 random --games 100 --width 15 --height 15 --maxTurns 500 --maxTime 30000

# 对比 StrategyRL 和 Adaptive（需要指定Q表路径）
node src/training/train.js compare --ai1 strategy_rl --ai2 adaptive --qtableP1 ./qtable-1v1-seq3.json --games 100 --width 15 --height 15 --maxTurns 500 --maxTime 30000
```

---

## 项目功能概述

### 核心游戏功能

- ✅ **公平地图生成**：基于 Voronoi 图的公平地图生成算法
  - 确保各玩家初始地盘大小差异 < 20%
  - 首都之间最小距离控制
  - 区域内资源（要塞、山区）均衡分配
  - 连通性保证（允许最多20%不连通）

- ✅ **回合制游戏引擎**：完整的游戏逻辑实现
  - 军队增长系统（普通格子、要塞、首都）
  - 战斗系统（占领、平局、失败）
  - 移动系统（点击+点击、点击+拖拽）
  - 胜利判定（占领首都或消灭所有军队）
  - 首都迁移机制

### AI 智能体系统

项目实现了5种不同层次的AI智能体：

1. **RandomAI**（随机AI）
   - 基础难度，随机选择移动
   - 适合新手练习和基准测试

2. **AdaptiveAI**（自适应AI）
   - 多策略评估机制（进攻、防御、发育）
   - 智能策略切换和权重平衡
   - 高胜率表现（1v1对Random约95%+）

3. **MinimaxAI**（Minimax算法AI）
   - 基于 Minimax 算法和 Alpha-Beta 剪枝
   - 多维度评估函数（领土、军队、重要目标等）
   - 可配置搜索深度和分支数
   - 适合人工智能课程学习

4. **QLearningAI**（基础Q-Learning AI）
   - 直接学习动作级别的Q值
   - 简化状态空间（2维特征）
   - 适合研究基础强化学习算法

5. **StrategyRLAI**（策略强化学习AI）⭐ **最强AI**
   - 分层架构：高层策略选择（RL）+ 底层战术执行（启发式）
   - 使用 Q-Learning 学习4种高层策略（ATTACK、DEFEND、GROW、ALL_IN）
   - 底层执行器：BFS路径规划、多源攻击、主动防御等
   - 状态空间压缩（5维特征，18,000状态）
   - **性能表现**：
     - 1v1对Random：胜率 ~100%
     - 1v1对Adaptive：胜率 ~99%
     - 1v3对Adaptive：胜率 ~75%

### 训练和评估系统

- ✅ **游戏模拟器**：无头模式运行游戏，收集统计数据
- ✅ **多维度评估器**：评估AI的胜负、效率、稳定性等指标
- ✅ **命令行工具**：支持evaluate、compare、benchmark三种命令
- ✅ **数据集分离**：训练集、验证集、测试集分离
- ✅ **对抗性测试**：被围攻、资源劣势、长期对抗等场景
- ✅ **稳定性评估**：标准差、变异系数、分位数分析

### 前端界面

- ✅ **游戏主菜单**：配置玩家、地图尺寸、AI类型
- ✅ **Canvas渲染**：流畅的游戏画面
- ✅ **游戏UI**：当前玩家、回合数、排行榜
- ✅ **暂停菜单**：退出游戏功能
- ✅ **缩放和重置视角**：支持鼠标滚轮缩放
- ✅ **响应式设计**：适配不同屏幕尺寸

### 技术特性

- ✅ **现代化技术栈**：React 18 + Vite + Canvas + Zustand + Tailwind CSS
- ✅ **前端集成RL模型**：训练好的Q表自动加载到前端
- ✅ **模块化AI架构**：易于扩展新的AI类型
- ✅ **完整的训练框架**：支持强化学习训练和评估

---

## 项目结构

```
generals.io/
├── src/
│   ├── components/          # React组件（游戏界面）
│   ├── game/                # 游戏核心逻辑
│   │   ├── GameEngine.js    # 游戏引擎
│   │   ├── MapGenerator.js  # 地图生成器
│   │   ├── Renderer.js      # 渲染引擎
│   │   └── ai/              # AI系统
│   │       ├── RandomAI.js
│   │       ├── AdaptiveAI.js
│   │       ├── MinimaxAI.js
│   │       ├── StrategyRLAI.js
│   │       ├── QLearningAI.js
│   │       └── StrategyExecutors.js  # RL策略执行器
│   ├── training/            # AI训练和评估系统
│   │   ├── tournament.js    # 锦标赛脚本
│   │   ├── test_vs_random.js # AI vs RANDOM测试
│   │   ├── analyze_tournament.js # 结果分析工具
│   │   ├── train.js         # 命令行工具
│   │   ├── train_rl.js      # RL训练工具
│   │   ├── train_qlearning.js # QLearning训练脚本
│   │   ├── RLTrainer.js     # RL训练器
│   │   ├── GameSimulator.js # 游戏模拟器
│   │   └── Evaluator.js     # 评估器
│   ├── store/               # 状态管理
│   └── assets/              # 资源文件
├── public/                  # 静态资源（包含Q表文件）
├── dist/                    # 构建输出
└── package.json
```

---

## 技术文档

### 详细文档

- **训练系统文档**：[src/training/README.md](./src/training/README.md)
- **命令详解**：[src/training/COMMANDS.md](./src/training/COMMANDS.md)
- **评估指标说明**：[src/training/METRICS.md](./src/training/METRICS.md)
- **RL策略文档**：[src/training/RL_STRATEGY.md](./src/training/RL_STRATEGY.md)
- **项目技术报告**：[PROJECT_REPORT.md](./PROJECT_REPORT.md)

### 游戏规则

#### 基本玩法

1. **地图**：网格地图，包含空地、山脉、要塞和首都
2. **军队**：每个己方格子每回合自动增长军队
3. **移动**：从己方格子移动到相邻格子（需要至少2个军队）
4. **战斗**：移动到敌方格子会触发战斗
5. **胜利**：最后一个存活的玩家获胜（占领敌方首都或消灭所有敌方军队）

#### 详细规则

- **军队增长**：
  - 普通格子：每25回合（1轮）+1单位
  - 要塞：每回合+1单位
  - 首都：每回合+1单位

- **战斗机制**：
  - 攻击方 > 防御方：占领，剩余 = 攻击方 - 防御方
  - 攻击方 = 防御方：双方都消失，格子变为空地
  - 攻击方 < 防御方：攻击失败，防御方剩余 = 防御方 - 攻击方

- **移动规则**：
  - 只能移动到相邻的上下左右四个方向
  - 不能移动到山区
  - 移动时必须在原格子留下至少1个单位
  - 支持两种移动方式：
    - **点击+点击**：移动50%兵力（向下取整）
    - **点击+拖拽**：只保留1个单位，其余全部移动

---

## 部署说明

### 本地构建

```bash
npm run build
```

构建完成后，`dist/` 目录包含：
- 编译后的前端代码
- Q表文件（`qtable-strategy.json`、`qtable-qlearning.json`）
- 静态资源

### 部署到服务器

1. **构建项目**：运行 `npm run build`
2. **部署 dist 目录**：将 `dist/` 目录部署到任何静态文件服务器
3. **确保Q表文件存在**：`qtable-strategy.json` 和 `qtable-qlearning.json` 必须在 `dist/` 目录中
4. **访问**：通过浏览器访问部署的 `index.html`

**注意**：RL AI 需要加载 Q 表文件，确保这些文件可以通过 `/qtable-strategy.json` 和 `/qtable-qlearning.json` 路径访问。

---

## 开发计划

- [ ] 游戏回放功能
- [ ] 统计信息（胜率、平均回合数等）
- [ ] 快捷键支持
- [ ] 音效和背景音乐
- [ ] 更多AI策略类型
- [ ] 迷雾系统（视野限制）

详细开发计划请查看 [PROJECT_PLAN.md](./PROJECT_PLAN.md)

---

## 贡献

欢迎提交 Issue 和 Pull Request！

---

## License

MIT

---

## 参考资料

- [Generals.io 官方网站](https://generals.io)
- [Generals.io 游戏规则](https://generals.io/help)

---

**注意**: 这是一个独立开发的游戏项目，与官方的 generals.io 无关。
